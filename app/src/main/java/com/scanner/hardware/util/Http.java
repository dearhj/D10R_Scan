package com.scanner.hardware.util;

import com.scanner.hardware.BuildConfig;

public class Http {
    public static void init(){
        //\u000dSystem.out.println("混沌初开，乾坤始奠。--v" + BuildConfig.VERSION_NAME);
    }
}